# Archive root

Material moved here during `feat/cleanup-legacy-scripts-main-page`. **Not part of the Next.js build** (`tsconfig` excludes `_archive`).

| Folder | Contents |
|--------|----------|
| `legacy-scripts/` | Root one-off codemods, smoke tests, log dumps |
| `unused-components/` | Orphan sections, UI, demo routes, TEMP stores |
| `liquid-buttons/` | Standalone CSS/JS lab (not imported by the app) |

**Do not hard-delete** until production is stable and the team confirms. Restore by moving files back to their original paths (see each folder README).
