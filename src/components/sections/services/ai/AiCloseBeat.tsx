"use client";

/**
 * ACT 7 driver — the peak-end beat.
 *
 * Renders nothing beyond its wrapper. The persistent neural field continues
 * behind the closing CTA without a second canvas or scroll driver.
 */

export default function AiCloseBeat({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
